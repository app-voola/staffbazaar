import { CreateProfileClient } from './CreateProfileClient';

export const metadata = { title: 'Create Your Profile — StaffBazaar' };

export default function CreateProfilePage() {
  return <CreateProfileClient />;
}
